{
  "buildCommand": "cd client && npm install && ./node_modules/vite/bin/vite.js build && cd ../server && npm install && npm run build",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/server/dist/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}